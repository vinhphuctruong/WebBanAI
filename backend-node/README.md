# Backend Node.js

## Stack
- Express
- PostgreSQL (`pg`)
- MongoDB (`mongoose`)
- JWT auth

## Run
```bash
cp .env.example .env
npm install
npm run dev
```

## Notes
- Tu dong tao schema Postgres va seed user khi khoi dong.
- Tu dong seed content Mongo (`gems`, `ai_tools`, `reviews`, `pricing`).
